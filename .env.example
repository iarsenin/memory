# Copy this to .env and fill in values. Never commit .env to git.

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
TIMEZONE=America/New_York

# Optional — only needed if using Gmail notifications
GMAIL_USER=
GMAIL_APP_PASSWORD=
EMAIL_TO=
EMAIL_FROM=
